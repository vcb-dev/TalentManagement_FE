{
  "openapi": "3.1.0",
  "info": { "title": "VCB HRM API", "version": "1.0.0" },
  "paths": {
    "/auth/google": {
      "get": {
        "summary": "Bắt đầu OAuth Google (redirect)",
        "responses": {
          "302": { "description": "Redirect tới Google" }
        }
      }
    },
    "/auth/google/callback": {
      "get": {
        "summary": "Google OAuth callback (redirect, set cookie)",
        "parameters": [
          { "name": "code", "in": "query", "schema": { "type": "string" } },
          { "name": "state", "in": "query", "schema": { "type": "string" } }
        ],
        "responses": {
          "302": { "description": "Redirect về FRONTEND_URL/login?oauth=success|error" }
        }
      }
    },
    "/auth/logout": {
      "post": {
        "summary": "Đăng xuất (xóa cookie session)",
        "responses": {
          "200": { "description": "OK" }
        }
      }
    },
    "/auth/me": {
      "get": {
        "summary": "Phiên hiện tại",
        "security": [{ "bearerAuth": [] }],
        "responses": {
          "200": {
            "description": "Session",
            "content": {
              "application/json": {
                "schema": { "$ref": "#/components/schemas/MeResponse" }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "bearerAuth": { "type": "http", "scheme": "bearer", "bearerFormat": "JWT" }
    },
    "schemas": {
      "Role": {
        "type": "string",
        "enum": ["MEMBER", "LEADER", "MANAGER", "HR", "TEACHER", "BOD"]
      },
      "Permission": {
        "type": "object",
        "required": ["action", "resource"],
        "properties": {
          "action": {
            "type": "string",
            "enum": ["view", "create", "edit", "deactivate", "grade", "approve", "classify"]
          },
          "resource": {
            "type": "string",
            "enum": [
              "employee",
              "exam",
              "promotion",
              "team",
              "department",
              "radar_chart",
              "checklist",
              "submission",
              "kpi",
              "okr",
              "monthly_report"
            ]
          }
        }
      },
      "UserSession": {
        "type": "object",
        "required": ["id", "name", "email", "role", "permissions", "departmentId", "teamIds"],
        "properties": {
          "id": { "type": "string" },
          "name": { "type": "string" },
          "email": { "type": "string", "format": "email" },
          "role": { "$ref": "#/components/schemas/Role" },
          "roles": {
            "description": "Đa vai trò (RBAC động); khi có thì union quyền theo các role + permissionIds.",
            "type": "array",
            "items": { "$ref": "#/components/schemas/Role" }
          },
          "permissions": {
            "type": "array",
            "items": { "$ref": "#/components/schemas/Permission" }
          },
          "permissionIds": {
            "description": "Quyền chức năng theo id catalog (chuỗi ổn định, ví dụ hr.employees.view).",
            "type": "array",
            "items": { "type": "string" }
          },
          "dataScopeFlags": {
            "description": "Cờ giới hạn dữ liệu (ABAC), ví dụ chỉ xem bản ghi phụ trách.",
            "type": "object",
            "additionalProperties": { "type": "boolean" }
          },
          "departmentId": { "type": "string", "format": "uuid" },
          "teamIds": {
            "type": "array",
            "items": { "type": "string", "format": "uuid" }
          }
        }
      },
      "MeResponse": {
        "type": "object",
        "required": ["user"],
        "properties": {
          "user": { "$ref": "#/components/schemas/UserSession" },
          "accessToken": { "type": "string" }
        }
      },
      "Employee": {
        "type": "object",
        "required": [
          "id",
          "name",
          "email",
          "role",
          "status",
          "departmentId",
          "teamIds",
          "currentLevel",
          "currentStar",
          "createdAt",
          "updatedAt"
        ],
        "properties": {
          "id": { "type": "string", "format": "uuid" },
          "name": { "type": "string" },
          "email": { "type": "string", "format": "email" },
          "role": { "$ref": "#/components/schemas/Role" },
          "status": {
            "type": "string",
            "enum": ["ACTIVE", "INACTIVE", "PROBATION", "RESERVED"]
          },
          "departmentId": { "type": "string", "format": "uuid" },
          "teamIds": {
            "type": "array",
            "items": { "type": "string", "format": "uuid" }
          },
          "currentLevel": {
            "type": "string",
            "enum": ["tap_su", "biet_viec", "duoc_viec", "dong_gop_ket_qua", "tuong"]
          },
          "currentStar": { "type": "integer", "minimum": 0, "maximum": 6 },
          "createdAt": { "type": "string", "format": "date-time" },
          "updatedAt": { "type": "string", "format": "date-time" },
          "phone": { "type": "string", "nullable": true },
          "birthDate": { "type": "string", "nullable": true },
          "startDate": {
            "type": "string",
            "nullable": true,
            "description": "Ngày bắt đầu (start_date_work)"
          }
        }
      },
      "CreateEmployeeInput": {
        "type": "object",
        "required": ["name", "email", "role", "departmentId", "teamId"],
        "properties": {
          "name": { "type": "string", "minLength": 2 },
          "email": { "type": "string", "format": "email" },
          "role": { "$ref": "#/components/schemas/Role" },
          "departmentId": { "type": "string", "format": "uuid" },
          "teamId": { "type": "string", "format": "uuid" },
          "extraTeamIds": {
            "type": "array",
            "items": { "type": "string", "format": "uuid" },
            "description": "Nhóm bổ sung (ngoài teamId theo phòng ban)"
          },
          "phone": { "type": "string" },
          "birthDate": { "type": "string" },
          "startDate": {
            "type": "string",
            "description": "Ngày bắt đầu làm việc (start_date_work)"
          },
          "initialLevel": {
            "type": "string",
            "enum": ["tap_su", "biet_viec", "duoc_viec", "dong_gop_ket_qua", "tuong"]
          },
          "notifyEmail": {
            "type": "boolean",
            "description": "false = không gửi email thông báo tạo tài khoản"
          },
          "displayName": { "type": "string" },
          "gender": { "type": "string" },
          "facebookUrl": { "type": "string" },
          "jobTitle": { "type": "string" },
          "teamPosition": { "type": "string" },
          "workplaceBranch": { "type": "string" },
          "directManager": { "type": "string", "nullable": true },
          "addressCurrent": { "type": "string" },
          "addressHousehold": { "type": "string" },
          "educationLevel": { "type": "string" },
          "schoolName": { "type": "string" },
          "hometownDetail": { "type": "string" },
          "identityDocumentInfo": { "type": "string" },
          "maritalStatus": { "type": "string" },
          "ethnicity": { "type": "string" },
          "religion": { "type": "string" },
          "insuranceBookNumber": { "type": "string" },
          "childrenInfo": { "type": "string" },
          "emergencyContact1": { "type": "string" },
          "emergencyContact2": { "type": "string" },
          "fatherGuardianContact": { "type": "string" },
          "motherGuardianContact": { "type": "string" },
          "familyNotes": { "type": "string" },
          "bankAccountInfo": { "type": "string" },
          "vehicleInfo": { "type": "string" },
          "managerBlockCode": { "type": "string" },
          "attachmentIdFront": { "type": "string" },
          "attachmentIdBack": { "type": "string" },
          "policyAcknowledgement": { "type": "string" },
          "profileReviewDate": { "type": "string" },
          "cvAttachmentRef": { "type": "string" },
          "notes": { "type": "string" }
        }
      },
      "PatchEmployeeInput": {
        "type": "object",
        "properties": {
          "name": { "type": "string", "minLength": 2 },
          "email": { "type": "string", "format": "email" },
          "role": { "$ref": "#/components/schemas/Role" },
          "departmentId": { "type": "string", "format": "uuid" },
          "teamId": { "type": "string", "format": "uuid" },
          "phone": { "type": "string" },
          "birthDate": { "type": "string" },
          "startDate": { "type": "string", "description": "Ngày bắt đầu làm việc" },
          "secondaryTeamId": {
            "type": "string",
            "description": "UUID team phụ trên FE; gửi chuỗi rỗng để xóa"
          },
          "currentLevel": {
            "type": "string",
            "enum": ["tap_su", "biet_viec", "duoc_viec", "dong_gop_ket_qua", "tuong"]
          },
          "status": {
            "type": "string",
            "enum": ["ACTIVE", "INACTIVE", "PROBATION", "RESERVED"]
          }
        }
      },
      "ExamResult": {
        "type": "object",
        "required": ["id", "examId", "employeeId", "result", "classifiedAt"],
        "properties": {
          "id": { "type": "string", "format": "uuid" },
          "examId": { "type": "string", "format": "uuid" },
          "employeeId": { "type": "string", "format": "uuid" },
          "result": {
            "type": "string",
            "enum": ["DAT", "BAO_LUU", "CHO_HOC_LAI", "CHIA_TAY"]
          },
          "classifiedAt": { "type": "string", "format": "date-time" }
        }
      }
    }
  }
}
