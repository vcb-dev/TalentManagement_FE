/** Chiều rộng tối đa chuẩn cho nội dung trang — khớp với các trang hiện có (xem KpiOkrWorkspace, HrEmployeeProfile...). */
export const PAGE_MAX_WIDTH_CLASS = 'max-w-[1400px]'

/** Container chuẩn cho các màn hình đứng một mình (không nằm trong AppShell content wrapper). */
export const PAGE_CONTAINER_CLASS = `mx-auto ${PAGE_MAX_WIDTH_CLASS} px-3 md:px-4`
